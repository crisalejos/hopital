{% extends 'base.html.twig' %}

{% block title %}Edit Appointments{% endblock %}

{% block body %}
    <h1>Edit Appointments</h1>

    {{ include('appointments/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('appointments_index') }}">Liste de Rendez-vous</a>

    {{ include('appointments/_delete_form.html.twig') }}
{% endblock %}
